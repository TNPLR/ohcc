#include <stdio.h>
#include <string.h>
#include <stdint.h>

int main(int argc, char* argv[]) {
  return 0;
}
int assembler(FILE *fPtr) {
  uint64_t tmp;
  char read[32];
  while (!feof(fPtr)) {
    fscanf("%s", read);
    if (strcmp(read, ""))
  }
  return 0;
}
